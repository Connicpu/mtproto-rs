[package]
name = "mtproto"
version = "0.1.0"
authors = ["Connor Hilarides <connorcpu@live.com>"]
build = "build.rs"

[dependencies]
byteorder = "0.4"
chrono = "0.2"

[build-dependencies]
syntex = "0.22"
tl_macros = { path = "tl_macros" }
