[package]
name = "mtproto"
version = "0.1.0"
authors = ["Connor Hilarides <connorcpu@live.com>"]
build = "build.rs"
license = "MIT/Apache-2.0"

[dependencies]
byteorder = "0.4"
chrono = "0.2"
rust-crypto = "0.2"
crc = "1.2"

[dev-dependencies]
rand = "0.3"

[build-dependencies]
syntex = "0.25"
tl_macros = { path = "tl_macros" }
